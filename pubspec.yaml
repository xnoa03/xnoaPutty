name: mlkit_test
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.7.2


dependencies:
  flutter:
    sdk: flutter
  google_mlkit_object_detection: ^0.11.0 # 최신 버전 확인
  camera: ^0.10.5+9 # 최신 버전 확인, 플랫폼별 설정 필요
  google_mlkit_commons: ^0.6.0 # InputImageRotation 등 공통 유틸리티 사용
  isolate: '>=2.1.1 <4.0.0'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2


flutter:

  
  uses-material-design: true


